-- Mod template patterns — populated by tools/scrape_poe2db.py (Modifiers page).
-- Each entry: { en = "<Lua pattern>", ja = "<replacement with %1/%2/...>" }
-- Order matters: more specific patterns first.
return {
	patterns = {
	},
}
